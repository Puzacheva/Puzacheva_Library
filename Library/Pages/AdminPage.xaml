<Page x:Class="Library.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="370" d:DesignWidth="750"
      Title="Администратор"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Background="#d7b88a">
            <Button x:Name="BtnBorrowedBooks" Width="130" Content="Выданные книги" Margin="0,5,10,5" Click="BtnBorrowedBooks_Click"/>
            <Button x:Name="BtnBookList" Width="130" Content="Список книг" Margin="0,5,0,5" Click="BtnBookList_Click"/>
        </StackPanel>

        <Frame x:Name="AdminFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
